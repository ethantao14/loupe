ANTHROPIC_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
CLAUDE_MODEL=claude-haiku-4-5
FRONTEND_ORIGIN=http://localhost:3000

# Session pooler URI from Supabase, used to apply migrations
DATABASE_URL=

# Code execution is off by default. Executed code can read files by absolute
# path and reach the network, so only enable it if you accept that.
ENABLE_CODE_EXECUTION=false
